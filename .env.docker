# Django Settings
DEBUG=False
SECRET_KEY=django-production-secret-key-change-in-production
DJANGO_SETTINGS_MODULE=FamilyHub.settings.docker

# Docker Environment Detection
IS_DOCKER=True

# PostgreSQL Connection Settings (matching docker-compose.yml)
POSTGRES_DB=familyhub_db
POSTGRES_USER=familyhub_user
POSTGRES_PASSWORD=TempPass123!ChangeMe
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Security Settings
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Superuser Settings for Automatic Creation
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@familyhub.local
DJANGO_SUPERUSER_PASSWORD=admin123
