# ⚡ Quick PostgreSQL Development Environment Variables
# 🚀 5-minute setup for FamilyHub PostgreSQL 17.6 + pgAdmin

# PostgreSQL Configuration
POSTGRES_DB=familyhub
POSTGRES_USER=django
POSTGRES_PASSWORD=secretpass

# pgAdmin Configuration  
PGADMIN_EMAIL=admin@familyhub.local
PGADMIN_PASSWORD=admin123

# Redis Configuration (Optional)
REDIS_PASSWORD=redis123

# Django Database URL (for quick connection)
DATABASE_URL=postgresql://django:secretpass@localhost:5432/familyhub

# Quick Setup Notes:
# 1. Run: docker-compose -f docker-compose.quick.yml up -d
# 2. Access pgAdmin: http://localhost:5050 (admin@familyhub.local / admin123)
# 3. PostgreSQL: localhost:5432 (django / secretpass)
# 4. Add Redis: docker-compose -f docker-compose.quick.yml --profile full up -d
